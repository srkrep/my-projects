const initialState = {

}

console.log("Auth Reducer", initialState)

export default (state = initialState, action) => {
    switch (action.type) {


        default:
            return state;
    }
}